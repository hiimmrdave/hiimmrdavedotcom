<script webc:setup>
  // only use for current page, need to take input for what page it is
</script>
<time
  :datetime="page.date.toISOString();"
  @text="page.date.toLocaleDateString( 'en-US', { month: 'long', year:'numeric'});"
></time>
